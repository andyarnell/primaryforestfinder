[general]
name=Primary Forest Finder
qgisMinimumVersion=3.10
qgisMaximumVersion=3.99
description=Processing tools for delineating primary forest candidate areas using anthropogenic influence, slope and protected area tiers.
version=0.16.0-beta.17
changelog=
    0.16.0-beta.17 — Renamed the 02a tree-cover input from "forest_raw" to "tree_cover_binary" so it's no longer confused with the raw Hansen/GLAD source datasets. The plugin still auto-matches already-exported 02a_forest_raw files.
    0.16.0-beta.16 — Input-category section aligned to the GEE app: removed "Primary forest" from the dropdown, renamed the prompt to "Treat input as:", placeholder to "Non FRA aligned", and added an ⓘ button with a tabbed FRA Definitions / Hierarchy popup. Dock fix: the output-log pane no longer takes ~half the height when side-docked, so the sections fill the panel.
    0.16.0-beta.15 — Run summary no longer mislabels harmless GDAL warnings (e.g. the DEM NoData notice) as fatal errors; auto-handled notices (geographic-CRS reproject) now render as blue informational, not red.
    0.16.0-beta.14 — Target CRS could be silently dropped (editing ISO3/AOI cleared a chosen CRS) or left empty, crashing national runs in Refine Output. Reverted to manual CRS input + run/algorithm CRS guards.
author=Andy Arnell
email=
about=Replicates the PFF Google Earth Engine app workflow using local data with GDAL and native QGIS Processing algorithms. Includes validate, prepare, distance, anthropogenic mask, primary forest, refine output, and zonal statistics tools — plus a one-click full workflow runner with selective exports, auto UTM, and combined coded raster output.
tracker=https://github.com/andyarnell/primaryforestfinder/issues
repository=https://github.com/andyarnell/primaryforestfinder
tags=forest,conservation,primary forest,raster,analysis
homepage=https://github.com/andyarnell/primaryforestfinder
category=Analysis
icon=
experimental=True
deprecated=False
hasProcessingProvider=yes
